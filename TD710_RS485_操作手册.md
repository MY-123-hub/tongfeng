# TD710 变频器 RS485 操作与调试手册

**适用项目：** 通风系统 TD710 变频器，电脑通过 USB-RS485 转换器和 Modbus Poll（或串口助手）完成参数配置、试运行与排障。

**现场已验证通信配置：** 从站地址 `1`，`9600 bps`，`8N1`，Modbus RTU。

**本手册定位：** 合并《TD710_RS485_现场操作手册》与《TD710_RS485_电脑初次调试教程》，并订正其中的错误（主要是运行/停机命令的写法）。所有地址、命令值、CRC 均已对照 TD710 官方说明书逐项核实。

> [!DANGER]
> 变频器含危险电压与旋转机械风险。上电、改接线、调谐、运行前，必须确认风机周围无人、叶轮无异物、急停有效。严禁带电插拔跳帽、拆装 RS485 线或更换电机接线。初次试转前由具备低压电气作业资格的人员确认后方可执行。

---

## 0. 先记住这两条，其余内容围绕它们展开

1. **通信参数固定为：地址 `1`、`9600 bps`、`8N1`、Modbus RTU**（TD710 上对应 F11.02=`2`、F11.03=`0`）。
2. **运行/停机命令不能用功能码 `06` 单独写 `0x2000`。** 真机会回 `Exception error 8`（上位机控制命令无效）。必须用**功能码 `16`（`0x10`）同帧写 `0x2000`（运行命令）+ `0x2001`（频率）两个寄存器。官方说明书里 `01 06 20 00 00 01 43 CA` 这条"正转"示例在真机上不成立，**不要照抄**。

---

## 1. 所需物料与接线

### 1.1 物料

1. 已完成主回路接线、可正常上电的 TD710 变频器和电机。
2. 电脑一台。
3. USB-RS485 隔离转换器一只，确认驱动装好后在设备管理器能看到 COM 口。
4. 一对双绞线；干扰较强时用屏蔽双绞线。
5. Modbus 主站软件（Modbus Poll 或任意能发十六进制 RTU 帧的串口助手）。

### 1.2 RS485 接线

| USB-RS485 转换器 | TD710 端子 |
|---|---|
| `A` / `D+` / `485+` | `485+` |
| `B` / `D-` / `485-` | `485-` |

- 不要把 A/B 接到 TD710 的 `COM`、`GND`、`24V`、`10V`。
- 不要把 RS485 线与电机 U/V/W 动力线同槽长距离平行敷设。
- 若参数全部正确仍完全无回应，**只在变频器断电后**对调两根信号线再复测，不要带电拆装。

### 1.3 JP2 终端电阻

| 场景 | JP2 位置 |
|---|---|
| 电脑直连一台变频器、总线较短 | `ON` |
| 多台变频器共用一条总线 | **仅物理总线两端**置 `ON`，中间设备全部 `OFF` |

不确定当前跳帽状态时，先断开变频器输入电源再检查。

### 1.4 其他跳帽（JP3 / JP4 / JP5）

这三只用于模拟量信号选择，与 RS485 无关，但不要把全部跳帽清空：

| 跳帽 | 作用 | 电流档 | 电压档 |
|---|---|---|---|
| JP3 | AI1 输入 | `Cin`：4–20 mA | `Vin`：0–10 V |
| JP4 | AO1 输出 | `Co1`：4–20 mA | `Vo1`：0–10 V |
| JP5 | AO2 输出 | `Co2`：4–20 mA | `Vo2`：0–10 V |

AI1/AO1/AO2 均未接线时，可临时设为 `JP3=Vin`、`JP4=Vo1`、`JP5=Vo2`。

---

## 2. 面板参数设置（一次性）

面板操作规律：`PRG` 进入/退出；`▲/▼` 改值（移位键切换要改的数位）；`ENTER` 才写入并自动转到下一参数；只按 `PRG` 返回**不保存**。数值不闪烁表示该参数不可改或要求停机后改。

每一行都按"选中功能码 → ENTER → 改值 → ENTER 保存"执行。带 `★` 的是 RS485 控制不可缺少的项。

### 2.1 基本运行参数

|  顺序 | 参数       |        设定值 | 说明                       |
| --: | -------- | ---------: | ------------------------ |
|   1 | F00.01   |        `0` | 通用模式                     |
|   2 | F00.02   |        `2` | 开环电流矢量控制（SVC）            |
|   3 | F00.03 ★ |        `2` | 运行命令通道 = 通讯运行命令通道        |
|   4 | F00.04 ★ |        `2` | 主频率源 A = 数字给定 3（通讯设定）    |
|   5 | F00.06 ★ |        `0` | 频率源给定方式 = 仅用主频率源 A       |
|   6 | F00.12   | `50.00 Hz` | 最大输出频率，不得超电机/风机允许值       |
|   7 | F00.13   | `50.00 Hz` | 上限频率，可按项目限速降低            |
|   8 | F00.14   |  `0.00 Hz` | 下限频率                     |
|   9 | F00.16   |   `15.0 s` | 加速时间，可按机械惯量调大            |
|  10 | F00.17   |   `15.0 s` | 减速时间，减速过压时调大             |
|  11 | F00.18   |        `0` | 正转。转向确认后再定，不要用软件反转代替电气核对 |
|  12 | F01.08   |        `0` | 停机方式 = 减速停机              |
|  13 | F04.10   |        `0` | 速度控制（不要设转矩控制）            |

### 2.2 通信参数（F11 组）

| 参数 | 设定值 | 说明 |
|---|---:|---|
| F11.00 ★ | `0` | 协议 = MODBUS |
| F11.01 ★ | `1` | 本机地址 = 1（`0` 是广播地址，不能作单台查询对象） |
| F11.02 ★ | `2` | 波特率 = 9600 bps（`0`=2400、`1`=4800、`2`=9600、`3`=19200） |
| F11.03 ★ | `0` | 数据格式 = 无校验、8 数据位、1 停止位，即 **8N1** |
| F11.04 | `5 ms` | 本机应答延时，保留默认 |
| F11.05 | `0` | 写操作有回应（初调必须保留回应） |
| F11.06 | `1.00` | 比例联动系数，单机保持 1.00 |

> [!NOTE]
> 旧资料里 F11.02 写 `3`=9600 是错的（`3` 是 19200），已订正为 `2`。F11.03 出厂默认 `1`（偶校验 E,8,1），本流程统一改 `0`（8N1），电脑端必须一致。

### 2.3 电机铭牌参数（F02 组，首次运行前必填）

按下表**逐字抄现场电机铭牌**，不要照搬其他电机数据：

| 参数 | 填写规则 |
|---|---|
| F02.00 | `0` = 交流异步电机 |
| F02.01 | 额定功率（kW） |
| F02.02 | 额定频率（Hz） |
| F02.03 | 额定转速（r/min） |
| F02.04 | 额定电压（V） |
| F02.05 | 额定电流（A） |

### 2.4 通信断线安全策略（二选一并记录）

| 现场要求 | F10.24 | F10.25 |
|---|---:|---:|
| 失联后按减速停机（普通通风设备默认） | `2` | `5.0 s` |
| 失联后维持当前运行（仅连续工艺要求时） | `1` | `5.0 s` |

**默认取第一行**。不要把 F10.25 设为 `0.0`（表示关闭通信超时检测）。最终选择记入第 9 节验收单。

---

## 3. 电机静态调谐

先确认电机接线正确、风机周围安全，再执行：

1. 设 `F02.16=1`，面板出现 `-STR-`。
2. 将 `F00.03` 临时设为 `0`，按 ENTER 保存（静态调谐期间用面板控制）。
3. 在 `-STR-` 状态按面板 `RUN`，等待调谐结束。
4. 按 `STOP/RESET`，将 `F00.03` 改回 `2` 并保存。
5. 若出现 `E-21`，停止操作，核对 F02.01～F02.05 与铭牌、电机接线、变频器容量。

> 若风机允许安全空载转动，可改用 `F02.16=2`（空载完整调谐，电机会转）；这不是静态调谐的等效替代，仅在安全条件满足时执行。

---

## 4. Modbus Poll 建立连接

按 `F3`，选择 **Serial Port**，按下表设置：

| 项目 | 值 |
|---|---|
| Port | USB-RS485 对应的 `COMx` |
| Mode | RTU |
| Baud rate | `9600` |
| Data bits | `8` |
| Parity | `None` |
| Stop bits | `1` |
| Timeout | `1000 ms` |

> Modbus Poll 里 **Connection 必须选 "Serial Port"**（不要选 "RS485"）；若 USB-RS485 转换器靠 RTS 切换收发方向，把 RTS Control 设为 **RTS Toggle**，否则只发不收。

Modbus Poll 地址从 0 开始计数，TD710 十六进制地址与软件十进制地址对应如下：

| 用途 | TD710 地址 | Modbus Poll Address |
|---|---:|---:|
| 运行命令 | `2000H` | `8192` |
| 通信频率 | `2001H` | `8193` |
| 监控参数 d-xx | `D000H`（与 `1D00H` 通用） | `53248` / `7424` |
| 运行状态 | `A000H`（与 `1A00H` 通用） | `40960` |
| 故障码 | `E000H`（与 `1E00H` 通用） | `57344` |

---

## 5. 建立并保存常用窗口

建议建三个窗口分别保存为 `01_通信自检`、`02_频率设定`、`03_运行控制`。

### 5.1 通信自检窗口

`F8` → Slave ID=`1`、Function=`03 Read Holding Registers`、Address=`3`、Quantity=`1`、Scan Rate=`1000 ms`。

返回值必须是 `2`（即 F00.03=2），这一步验证的是"电脑→变频器、地址、通信格式"整条链路。

### 5.2 频率设定窗口

`F8` → Slave ID=`1`、Function=`06 Write Single Register`、Address=`8193`、Quantity=`1`、Scan Rate=`0 ms`。

频率换算：**写入值 = 目标频率(Hz) × 100**。

| 目标频率 | 写入值（十进制） | 十六进制 |
|---|---:|---|
| 5.00 Hz | 500 | 01F4H |
| 10.00 Hz | 1000 | 03E8H |
| 20.00 Hz | 2000 | 07D0H |
| 30.00 Hz | 3000 | 0BB8H |
| 40.00 Hz | 4000 | 0FA0H |
| 50.00 Hz | 5000 | 1388H |

> 频率寄存器 `2001H` **可以**用功能码 `06` 单独写；单独写频率不会启动风机。

### 5.3 运行控制窗口 ★ 核心

`F8` → Slave ID=`1`、Function=`16 Write Multiple Registers`、Address=`8192`、Quantity=`2`、Scan Rate=`0 ms`。

窗口中有两个数据格，按顺序填写：

| 顺序 | 寄存器 | 填写内容 |
|---|---|---|
| 第 1 项 | `2000H` | 运行命令：`1` 正转、`9` 反转、`3` 减速停机、`4` 自由停机 |
| 第 2 项 | `2001H` | 当前目标频率值，按"Hz × 100"填写 |

> [!WARNING]
> **`0x2000` 不能单独用功能码 `06` 写**（真机回 `Exception error 8`）。启停必须走本窗口（功能码 `16`，同帧写命令 + 频率）。

---

## 6. 寄存器地址与命令速查

### 6.1 控制命令（`2000H`）命令值

| 命令 | 值（十六进制） | 值（十进制） |
|---|---:|---:|
| 正转运行 | `0001H` | 1 |
| 反转运行 | `0009H` | 9 |
| 减速停机 | `0003H` | 3 |
| 自由停机 | `0004H` | 4 |
| 故障复位 | `0010H` | 16 |
| 正转点动 | `0002H` | 2 |
| 反转点动 | `000AH` | 10 |

命令字位定义（`2000H`）：`Bit2~Bit0`：`001`=运行、`010`=点动、`011`=停机、`100`=自由停机；`Bit3`：`0`=正转、`1`=反转；`Bit4`：`0`=无动作、`1`=复位。

### 6.2 监控参数与状态读取（功能码 `03`）

| 读取内容 | 地址 | 说明 |
|---|---|---|
| d-00 输出频率 | `D000H` / `1D00H` | 单位 0.01 Hz，÷100 得 Hz |
| d-01 设定频率 | `D001H` / `1D01H` | 单位 0.01 Hz |
| d-05 输出电流 | `D005H` / `1D05H` | 单位 0.1 A |
| d-08 电机转速 | `D008H` / `1D08H` | 单位 1 rpm |
| 运行状态 | `A000H` / `1A00H` | 位寄存器 |
| 故障代码 | `E000H` / `1E00H` | 见故障码表 |
| 预告警码 | `E001H` / `1E01H` | — |

运行状态（`A000H`）低字节关键位：`bit0`=`1` 运行；`bit1`=`0` 正转 / `1` 反转；`bit2~bit1`：`1` 加速、`2` 减速、`3` 匀速。

### 6.3 异常回应错误码

异常帧格式：`地址 + (0x80 + 功能码) + 错误码 + CRC`。

| 错误码 | 含义 |
|---|---|
| `01H` | 非法功能码 |
| `02H` | 非法地址 |
| `03H` | 非法数据 |
| `04H` | 非法寄存器长度 |
| `05H` | CRC 校验错误 |
| `06H` | 参数运行中不可修改 |
| `07H` | 参数不可修改 |
| `08H` | **上位机控制命令无效**（用 `06` 单写 `0x2000` 就会收到这个） |
| `09H` | 参数受密码保护 |
| `0AH` | 密码错误 |

---

## 7. 首次试运行：严格按顺序

### 7.1 自检

用"通信自检"窗口确认 F00.03 返回 `2`。

### 7.2 先写频率

用"频率设定"窗口先写 `1000`（10.00 Hz）。此时不应启动。

### 7.3 正转运行（功能码 16：命令 + 频率）

在"运行控制"窗口填：第 1 项=`1`、第 2 项=`1000`，执行一次写入。

观察项：风机在 F00.16 时间内加速到约 10 Hz，转向正确，无异常振动、摩擦声或过流告警。

### 7.4 停机（功能码 16：命令 + 频率）

在"运行控制"窗口填：第 1 项=`3`、第 2 项保持当前频率值，执行一次写入。

观察项：风机按 F00.17 的减速时间停机。

### 7.5 各档复测

10 Hz 通过后，依次用 20 / 30 / 40 Hz 复测，每档都验证正转与停机。每次都是**先写频率、再用功能码 16 发命令 + 频率**。

> 转向错误时：先停机、断开输入电源并等待直流母线放电完成后，再对调 U/V/W 中任意两相。不要用反转命令替代首次转向校正。

---

## 8. 原始 RTU 帧速查（串口助手 / 排障用）

用 Modbus Poll 时 CRC 由软件自动计算，无需手填；下表供串口助手直接发送或核对 Modbus Poll 实际发出的报文。帧尾为 CRC（低字节在前）。

### 8.1 读操作（功能码 `03`）

| 用途 | 帧 |
|---|---|
| 读 F00.03 | `01 03 00 03 00 01 74 0A` |
| 读运行状态 A000H | `01 03 A0 00 00 01 A6 0A` |
| 读故障码 E000H | `01 03 E0 00 00 01 B3 CA` |

### 8.2 单独写频率（功能码 `06`，只写 `2001H`，可单独用）

| 用途 | 帧 |
|---|---|
| 写 10.00 Hz | `01 06 20 01 03 E8 D3 74` |
| 写 20.00 Hz | `01 06 20 01 07 D0 D0 66` |
| 写 40.00 Hz | `01 06 20 01 0F A0 D6 42` |

### 8.3 运行/停机（功能码 `16`，同帧写 `2000H`+`2001H`，唯一正确写法）

| 用途 | 帧 |
|---|---|
| 正转 + 10.00 Hz | `01 10 20 00 00 02 04 00 01 03 E8 3B 10` |
| 停机 + 10.00 Hz | `01 10 20 00 00 02 04 00 03 03 E8 9A D0` |
| 正转 + 20.00 Hz | `01 10 20 00 00 02 04 00 01 07 D0 38 02` |
| 停机 + 20.00 Hz | `01 10 20 00 00 02 04 00 03 07 D0 99 C2` |
| 正转 + 30.00 Hz | `01 10 20 00 00 02 04 00 01 0B B8 3C EC` |
| 停机 + 30.00 Hz | `01 10 20 00 00 02 04 00 03 0B B8 9D 2C` |
| 正转 + 40.00 Hz | `01 10 20 00 00 02 04 00 01 0F A0 3E 26` |
| 停机 + 40.00 Hz | `01 10 20 00 00 02 04 00 03 0F A0 9F E6` |
| 正转 + 50.00 Hz | `01 10 20 00 00 02 04 00 01 13 88 36 F8` |

> 50.00 Hz 正转帧 `01 10 20 00 00 02 04 00 01 13 88 36 F8` 与官方说明书"正转并通讯给定频率 50HZ"示例完全一致，可作为整条链路自检基准。

---

## 9. 故障排查

| 现象 | 优先检查 |
|---|---|
| 软件无回应 / 超时 | COM 口、地址 `1`、9600/8N1、A/B 接线、JP2、屏蔽双绞线远离动力线 |
| F00.03 读回非 `2` | 面板重设 F00.03=`2`，按 ENTER 保存 |
| **Exception error 8** | 用了功能码 `06` 单写 `0x2000`；改用功能码 `16`（Address=8192、Quantity=2）同帧写命令+频率 |
| E-17（RS485 通讯故障） | 检查波特率、屏蔽/双绞线、A/B 极性、终端电阻、F10.25 超时设置 |
| E-21（电机调谐故障） | 核对 F02.01～F02.05、电机接线、变频器容量匹配 |
| 能设频率但不能运行 | 检查 F00.03=`2`、F00.04=`2`，且必须用"运行控制"窗口（功能码 16）发命令 |

---

## 10. 完工验收单

以下全部打钩才算完成初次调试：

- [ ] `485+ ↔ A/D+`、`485- ↔ B/D-` 接线正确；JP2 符合总线拓扑。
- [ ] F00.03=`2`、F00.04=`2`、F00.06=`0`。
- [ ] F11.00=`0`、F11.01=`1`、F11.02=`2`、F11.03=`0`、F11.05=`0`、F11.06=`1.00`。
- [ ] 已抄录现场电机铭牌；F02.01～F02.05 与铭牌一致。
- [ ] 已记录断线策略：F10.24=`____`，F10.25=`____ s`。
- [ ] 读取 F00.03 成功，返回值为 `2`。
- [ ] 10 / 20 / 30 / 40 Hz 正转、停机均通过；转向正确，无异常振动/报警。
- [ ] 运行/停机全部通过功能码 16 完成，未使用 `06` 单写 `0x2000`。
- [ ] 已保存三个 Modbus Poll 窗口配置或截图，交付下一位调试人员。

---

## 附录：原厂手册定位（TD710 说明书 V1.0）

- 端子 `485+ / 485-` 与 JP2：PDF 第 16 页（手册页码 -14）。
- 面板操作与参数保存：PDF 第 20～21 页（手册页码 -18～-19）。
- F00 基本运行参数：PDF 第 22～23 页（手册页码 -20～-21）。
- 电机参数、调谐：PDF 第 26～27 页（手册页码 -24～-25）。
- F10.24 / F10.25 与 F11 通信参数：PDF 第 46～47 页（手册页码 -44～-45）。
- Modbus 地址、读写帧、命令值、异常码：PDF 第 62～70 页（手册页码 -60～-68）。
