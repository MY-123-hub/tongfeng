
# 公共报文格式（正式报文格式）

```
[帧头][版本][类型][发送角色][发送组号][接收角色][接收组号]
[业务流水号][数据长度][数据区][CRC16]
```


| 字节位置    | 字段     | 长度   | 含义                       |
| ------- | ------ | ---- | ------------------------ |
| 0       | 帧头 1   | 1    | 固定 `AA`                  |
| 1       | 帧头 2   | 1    | 固定 `55`                  |
| 2       | 版本     | 1    | 固定 `01`                  |
| 3       | 报文类型   | 1    | 见第 3 节报文表                |
| 4       | 发送角色   | 1    | `01=控制室`、`02=主机`、`03=从机` |
| 5       | 发送组号   | 1    | 控制室固定 `00`；设备为 `01~04`   |
| 6       | 接收角色   | 1    | `01=控制室`、`02=主机`、`03=从机` |
| 7       | 接收组号   | 1    | 控制室固定 `00`；设备为 `01~04`   |
| 8       | 流水号低字节 | 1    | `flow_id` 低字节            |
| 9       | 流水号高字节 | 1    | `flow_id` 高字节            |
| 10      | 数据长度   | 1    | 后续数据区有多少字节               |
| 11 开始   | 数据区    | 0~96 | 各类报文的实际内容                |
| 最后 2 字节 | CRC16  | 2    | CRC-16/MODBUS，低字节在前      |

帧总长度为 `13~109` 字节。两端 LoRa UART 接收缓冲区必须不少于 `128` 字节；发送端不得构造数据区超过 `96` 字节的报文。

接收端需要检查帧头，版本是否正确，长度是否超限，CRC是否正确，接收角色和接收组号正确后才能对报文进行处理。

## 1.1 公共编码规则

- 所有多字节整数均为**小端序**。
- 温度统一为有符号 `int16_t`，单位 `0.1℃`。例如 `FA 00` = `250` = `25.0℃`。
- `0x8000`（`int16_t` 最小值）表示无效温度；`0x0000` 是合法的 `0.0℃`。
- 频率统一为无符号 `uint16_t`，单位 `0.01Hz`。例如 `B8 0B` = `3000` = `30.00Hz`。
- `flow_id` 为 `uint16_t` 小端序。同一业务的请求、转发、ACK、RESULT 必须使用同一个 `flow_id`；重发时保持不变。
- CRC 的计算范围是从帧头 `AA` 开始，直到数据区最后一个字节结束，不包含 CRC 本身；校验结果以低字节、再高字节写入帧尾。


# 2. 角色与组号

|设备|角色码|组号|
|---|---|---|
|控制室网关 G|`01`|`00`|
|主机 M1|`02`|`01`|
|主机 M2|`02`|`02`|
|主机 M3|`02`|`03`|
|主机 M4|`02`|`04`|
|从机 S1|`03`|`01`|
|从机 S2|`03`|`02`|
|从机 S3|`03`|`03`|
|从机 S4|`03`|`04`|

# 3. 报文表

| 类型值  | 报文名称              | 谁发给谁         | 作用           | 数据区内容                                              |
| ---- | ----------------- | ------------ | ------------ | -------------------------------------------------- |
| `01` | `READ_TEMP`       | 控制室→主机；主机→从机 | 请求一整套 36 点温度 | 读取模式                                               |
| `02` | `TEMP_36`         | 从机→主机；主机→控制室 | 一次返回 36 点温度  | 温度 1～温度 36，共 72 字节；每点 2 字节，单位 `0.1℃`；`0x8000` 表示无效温度 |
| `10` | `SET_FREQ`        | 控制室→主机       | 设置变频器频率      | 频率值                                                |
| `11` | `SET_TARGET_TEMP` | 控制室→主机       | 设置目标温度       | 温度值                                                |
| `12` | `MANUAL_RUN`      | 控制室→主机       | 手动启动风机       | 无                                                  |
| `13` | `MANUAL_STOP`     | 控制室→主机       | 手动停止并锁定停机    | 无                                                  |
| `14` | `SET_AUTO`        | 控制室→主机       | 解除手动状态，恢复自动  | 无                                                  |
| `15` | `QUERY_STATUS`    | 控制室→主机       | 查询当前状态       | 无                                                  |
| `20` | `ACK`             | 主机→控制室       | 命令已经收到       | 接收状态、拒绝原因                                          |
| `21` | `RESULT`          | 主机→控制室       | 命令最终执行结果     | 结果、模式、风机、频率、目标温度                                   |
| `7E` | `ERROR`           | 任意方向         | 处理失败、超时、组号错误 | 错误码、补充信息                                           |

## 3.1 数据区定义

| 报文类型 | 数据区格式 | 说明 |
| --- | --- | --- |
| `READ_TEMP` | `mode:u8` | `00=允许主机使用新鲜缓存`；`01=强制从机重新采样` |
| `TEMP_36` | `temperature[36]:int16_t` | 72 字节；顺序为 Port1~Port6，每端口 6 点 |
| `SET_FREQ` | `freq:uint16_t` | 单位 `0.01Hz` |
| `SET_TARGET_TEMP` | `target_temp:int16_t` | 单位 `0.1℃` |
| `MANUAL_RUN` / `MANUAL_STOP` / `SET_AUTO` / `QUERY_STATUS` | 无 | 数据长度必须为 `0` |
| `ACK` | `status:u8, reason:u8` | `status=0` 接收成功；非 0 表示拒绝。`reason` 见错误码表 |
| `RESULT` | `result:u8, mode:u8, fan_state:u8, freq:uint16_t, target_temp:int16_t` | 共 7 字节；`result=0` 表示最终执行成功 |
| `ERROR` | `error_code:u8, detail:u8` | 共 2 字节；用于协议、超时和设备异常 |

状态值约定：`mode` 为 `0=自动、1=手动运行、2=手动停机锁定`；`fan_state` 为 `0=停止、1=运行`。

错误码约定：`01=帧格式错误`、`02=CRC错误`、`03=组号或角色错误`、`04=不支持的命令`、`05=参数越界`、`06=设备离线`、`07=执行超时`、`08=变频器执行失败`。

主机缓存新鲜度暂定为 `3 秒`。控制室使用 `READ_TEMP` 的 `mode=00` 时，主机仅在缓存更新时间距当前不超过 3 秒时直接返回；否则必须向同组从机请求重新采样。

## 3.2 通信窗口与发送规则

同一 LoRa 信道为半双工媒介，任何设备不得在未被主机轮询或授权时自行发送。当前单组 `S1-M1-C` 的 1 秒周期如下：

| 相对时间 | 发送方向 | 报文 | 规则 |
| --- | --- | --- | --- |
| `0 ms` | M1 → S1 | `READ_TEMP(mode=00)` | 主机读取从机最近一次采样缓存 |
| `50~200 ms` | S1 → M1 | `TEMP_36` | 从机仅在该回复窗口发送；本阶段不在轮询后临时采样 |
| `220 ms` | M1 → C | `TEMP_36` 或状态报文 | 主机转发缓存或新采样数据 |
| `250~400 ms` | C → M1 | 控制命令、`READ_TEMP` 或空 | 控制室只在此窗口发送；触摸命令先缓存 |
| `450 ms` | M1 内部执行 | - | 变频器命令由主机执行，不转发给从机 |
| `700 ms` | M1 → C | `ACK` / `RESULT` | 主机反馈命令接收与最终结果 |
| `800~1000 ms` | - | - | 保护时间，随后进入下一周期 |

控制室在触摸屏产生命令时，只设置待发送标志和保存命令内容；收到主机上报后才能在控制室窗口发出。主机正在发送时不接收 LoRa 数据，但从机和控制室都被时隙限制，因此不会与主机发送冲突。
# 4. 报文示例

模拟场景为：

```
控制室 LoRa 请求 M1 温度；
M1 发现本机缓存过期；
M1 向同组 S1 请求新温度；
S1 返回 36 点；
M1 再把 36 点转给控制室。
```

本次业务流水号统一为 `100`，字节表示为：

```
64 00
```

角色码：

```
控制室 G = 01，组号 00
主机 M1 = 02，组号 01
从机 S1 = 03，组号 01
```

## 1. 控制室 LoRa → 主机 M1

控制室请求 M1 发送 36 点温度：

```
AA 55 01 01 01 00 02 01 64 00 01 00 50 0D
```

解析：

|字节|内容|含义|
|---|---|---|
|`AA 55`|帧头|一条报文开始|
|`01`|版本|当前协议版本 1|
|`01`|报文类型|`READ_TEMP`，请求温度|
|`01 00`|发送者|控制室 G，组号 00|
|`02 01`|接收者|主机 M1，组号 01|
|`64 00`|业务流水号|本次温度请求编号 100|
|`01`|数据长度|数据区 1 字节|
|`00`|读取模式|允许主机先使用缓存|
|`50 0D`|CRC16|本报文校验|

翻译：

```
控制室 → 主机1：
“请给我一整套36点温度，流水号100；
如果你的缓存还新鲜，可以直接发。”
```

## 2. 主机 M1 → 从机 S1

M1 发现缓存过期，要求 S1 重新采样：

```
AA 55 01 01 02 01 03 01 64 00 01 01 C0 C9
```

解析：

|字节|内容|含义|
|---|---|---|
|`AA 55`|帧头|一条报文开始|
|`01`|版本|协议版本 1|
|`01`|报文类型|`READ_TEMP`，请求温度|
|`02 01`|发送者|主机 M1，组号 01|
|`03 01`|接收者|从机 S1，组号 01|
|`64 00`|业务流水号|仍然是本次请求 100|
|`01`|数据长度|数据区 1 字节|
|`01`|读取模式|强制重新采样|
|`C0 C9`|CRC16|本报文校验|

翻译：

```
主机1 → 从机1：
“控制室正在等流水号100的温度，
请你重新采样36个温度并返回。”
```

## 3. 从机 S1 → 主机 M1

从机返回完整 36 点温度：

```
AA 55 01 02 03 01 02 01 64 00 48
FA 00 FB 00 FC 00 FD 00 FE 00 FF 00
00 01 01 01 02 01 03 01 04 01 05 01
06 01 07 01 08 01 09 01 0A 01 0B 01
0C 01 0D 01 0E 01 0F 01 10 01 11 01
12 01 13 01 14 01 15 01 16 01 17 01
18 01 19 01 1A 01 1B 01 1C 01 1D 01
B0 70
```

解析报文头：

| 字节       | 内容      | 含义                  |
| -------- | ------- | ------------------- |
| `AA 55`  | 帧头      | 一条报文开始              |
| `01`     | 版本      | 协议版本 1              |
| `02`     | 报文类型    | `TEMP_36`，完整 36 点温度 |
| `03 01`  | 发送者     | 从机 S1，组号 01         |
| `02 01`  | 接收者     | 主机 M1，组号 01         |
| `64 00`  | 业务流水号   | 本次温度请求 100          |
| `48`     | 数据长度    | 十六进制 `48` = 72 字节   |
| 后续 72 字节 | 温度 1~36 | 每个温度占 2 字节，单位 0.1℃  |
| `B0 70`  | CRC16   | 本报文校验               |

温度举例：

```
FA 00 = 0x00FA = 250 = 25.0℃
FB 00 = 251 = 25.1℃
FC 00 = 252 = 25.2℃
……
1D 01 = 0x011D = 285 = 28.5℃
```

这一帧模拟的 36 个温度为：

```
Port1：25.0、25.1、25.2、25.3、25.4、25.5℃
Port2：25.6、25.7、25.8、25.9、26.0、26.1℃
Port3：26.2、26.3、26.4、26.5、26.6、26.7℃
Port4：26.8、26.9、27.0、27.1、27.2、27.3℃
Port5：27.4、27.5、27.6、27.7、27.8、27.9℃
Port6：28.0、28.1、28.2、28.3、28.4、28.5℃
```

完整温度报文长度：

```
帧头及公共字段：11 字节
温度数据：72 字节
CRC16：2 字节

总长度：85 字节
```

## 4. 主机 M1 → 控制室 LoRa

M1 收到 S1 的温度后，更新自己的缓存，再转发给控制室：

```
AA 55 01 02 02 01 01 00 64 00 48
FA 00 FB 00 FC 00 FD 00 FE 00 FF 00
00 01 01 01 02 01 03 01 04 01 05 01
06 01 07 01 08 01 09 01 0A 01 0B 01
0C 01 0D 01 0E 01 0F 01 10 01 11 01
12 01 13 01 14 01 15 01 16 01 17 01
18 01 19 01 1A 01 1B 01 1C 01 1D 01
7F 92
```

与从机报文相比：

```
36 个温度数据完全不变；
业务流水号仍为100；
只改变发送者、接收者和 CRC。
```

报文头变化：

|字节|内容|含义|
|---|---|---|
|`02 01`|发送者|主机 M1，组号 01|
|`01 00`|接收者|控制室 G，组号 00|
|`64 00`|业务流水号|仍然是 100|
|`48`|数据长度|72 字节温度数据|
|后续 72 字节|温度 1~36|与从机发送的数据相同|
|`7F 92`|CRC16|主机转发后的新校验值|

翻译：

```
主机1 → 控制室：
“这是流水号100的36点温度。”
```

如果 M1 缓存新鲜，则第 2、3 步不存在：

```
控制室 → M1：READ_TEMP，流水号100
M1 → 控制室：TEMP_36，流水号100
```
